import styled from "@emotion/styled";

const Button = styled.button`
    background-color: yellow;
    color: red;
    font-size: 2rem;
`
export default Button;

// export default function Button({children}) {
//     return <button style={{backgroundColor: `green`, color: `yellow` }}>
//         {children}
//     </button>
// }